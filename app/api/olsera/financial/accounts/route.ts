import { getAccounts } from "@/lib/olsera-financial-client"; import { deduplicateFinancialAccounts, normalizeAccounts, validatePeriod } from "@/lib/olsera-financial-core"; import { guard, json, errorJson } from "../_shared";
export const runtime = "nodejs"; export const dynamic = "force-dynamic";
export async function GET(req: Request) { try { await guard(); const u = new URL(req.url), yearText = u.searchParams.get("year"), monthText = u.searchParams.get("month"), period = validatePeriod(yearText, monthText); const year = Number(yearText), month = Number(monthText); const pages = await getAccounts(year, month); const accounts = deduplicateFinancialAccounts(normalizeAccounts(pages[0]), normalizeAccounts(pages[1])); return json({ status: "success", period, total: accounts.length, accounts }); } catch (e) { return errorJson(e); } }
